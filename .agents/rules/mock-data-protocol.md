---
trigger: always_on
---

State & Data: We are currently scaffolding the frontend. Strictly use mock data, standard React state (useState), or dummy JSON arrays. Do not attempt to write backend database logic or API calls until explicitly told.