# Generic Worker

Handles FounderOS work that does not need a specialized builder, document, design, or communications worker.

Run once:

```bash
npm run worker:generic:once
```

Run continuously:

```bash
npm run worker:generic
```
